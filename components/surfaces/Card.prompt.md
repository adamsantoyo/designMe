Soft paper panel — the default grouping surface for controls and content.

```jsx
<Card padding={16}>…</Card>
<Card tone="raised">{avatar}</Card>
```

Tones: `flat` (default), `raised` (radial top highlight, used by the avatar card), `inset` (recessed). `padding` accepts px or any CSS value.
