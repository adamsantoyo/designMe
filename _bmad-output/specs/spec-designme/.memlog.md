---
topic: designMe — recognition-first, no-typing avatar + style explorer (Expo, web + iPad)
updated: 2026-07-02T23:05
---

- (direction) Create op: express distill of docs/ (build-kickoff, art-bible, art-prompts, avatar-engine, catalog-bible, firefly-workflow) + CLAUDE.md; input rich, no elicitation needed.
- (decision) art-bible §0 supersedes catalog-bible's file/format constraints (inline-SVG-only, PNG-dev-only retired); raster PNG parts ARE production assets; values (determinism, recognition, dignity, calm) still hold.
- (decision) Art lane LOCKED 2026-06-26: two-tone handcrafted (flat base + warm shadow + soft highlight), neutral masters recolored via multiply; re-rolling the lane re-rolls the whole catalog.
- (capability) CAP-1 Avatar Studio: avatar IS the menu — tap region → contextual tray → live swap with soft settle; shuffle/save/undo action row.
- (capability) CAP-2 This-or-that second door: app proposes two looks, user reacts; a few rounds → starting style; serves switch-scanning and cold start.
- (capability) CAP-3 Lookbook: saved looks as calm gallery; tap to wear again; local-only persistence (AsyncStorage).
- (capability) CAP-4 Randomized non-default start straight into play — no tutorial, no sign-up.
- (capability) CAP-5 Deterministic layered avatar engine: PNG compositor on fixed z-order (art-bible §4), multiply recolor of neutral masters, per-color bakes for hard cases; render = pure fn of state+catalog.
- (capability) CAP-6 Representative curated catalog: 14 skins/18 hair/16 garment palette-locked; assistive tech + skin features + cultural expression as ordinary options; vibes as recipes not baked images.
- (capability) CAP-7 Art pipeline: worksheet one-liners (art-prompts) or Firefly workflow → transparent registered full-frame parts → ingest.py → registry; missing asset → placeholder silhouette so app runs before art exists.
- (constraint) Recognition over recall: no required typing, sign-up, caregiver gate, tutorial/modal walls, scores, streaks, timers, flashing, autoplay audio, infinite scroll, per-tap latency.
- (constraint) A11y: WCAG AA, targets ≥48px (primary ≥64px), full keyboard + visible focus, honor reduced motion; lean on native iPadOS assistive features.
- (constraint) Representation: never a light skin default; gender-expansive (every item, every avatar; no gendered menus); non-evaluative bodies; never 'what flatters you'.
- (constraint) Determinism: same state → same pixels; randomness only in shuffle (writes state); no AI/network on interaction hot path; front-end only, AsyncStorage-only persistence.
- (constraint) One Expo codebase → web + iPad; Skia canvas (dev build) is the target, plain Image tintColor a stopgap that loses two-tone shading.
- (constraint) Palette lock: only art-bible §3 hexes; parts full-frame registered on 1024×1536 (bust 1024×1024); filenames = catalog ids; ids stable (persistence contract, 3D-safe semantic ids).
- (constraint) Art process: undressed state is a designed state (friendly mannequin, no anatomical definition); never evaluate/share a composite without a face; AAC/letterboard only items allowed text.
- (assumption) Initial catalog scope is sister-focused (CLAUDE.md): the active hair set in art-prompts/firefly docs; breadth targets from catalog-bible apply later.
- (assumption) Skia requiring an Expo dev build (not Expo Go) is an accepted distribution constraint.
- (assumption) Vibes remain the default entry point per catalog-bible even though build-kickoff centers the Studio; treated as complementary (vibes = tray/this-or-that content), not conflicting.
- (question) Catalog↔worksheet id rename pending — check-art-ids.mjs reports 21/61 mismatches; which side is canonical, app/src/dm.ts ids or art-prompts filenames?
- (question) Firefly workflow (verified 2026-07) vs ChatGPT worksheet — is Firefly now the primary art lane, demoting art-prompts PREFIX to fallback?
- (question) Multiply recolor crushes shading on deep tones (black hair, deepest skins) — is the planned ramp/SVG runtime fix a ship-blocker given 'never a light default'?
- (question) Are the 3 style exemplars (definedCurls/hoodie/wheelchair) and skin/base locked yet? Mass generation is gated on them (art-bible §8).
- (note) Wrapper-only content dropped from build-kickoff.md: the Claude Design kickoff prompt text and the Design→Code handoff process narrative (process metadata, not contract).
- (event) Self-validate pass 1 (coherence): PASS — all 7 CAPs carry intent+success; constraints all bend design; 6 non-goals; success signal demonstrable; ids unique; CAP-5/CAP-7 mechanism-level intents accepted because the mechanism IS the contract (engine/pipeline).
- (event) Self-validate pass 2 (preservation): PASS — build-kickoff fully absorbed (screens/states → CAP success criteria, non-negotiables → constraints, tech target → constraints); art/catalog/engine detail preserved via 7 adopted companions; 4 assumptions + 4 open questions logged.
- (event) Spec finalized: SPEC.md derived at _bmad-output/specs/spec-designme/ (create op, 7 capabilities).
